1.조건이 여러개일 시 변수에 담아라.

2주석이 설명을 대신해주지 않는다 -> 코드만 봐도 이해가 되야 좋은 코드다

3.함수가 하는 일에 대해서도 정확히 이름쓰기 ex)renderAndGetValue
4.코드 줄수를 줄이는데 집착하지 말자
5.상수는 이름을 주자
6.깊이 잇는 중첩을 피하자 ex) if문같은 경우를 최대한 중첩하지 말자
깔끔하게 먼저 쳐낼수 잇는것을 앞에 써놓자
(early return패턴) -최대한 else를 쓰지말자 if가아니면 무조건 else다.


7.명명된 매개변수 사용
getLocation({lat:2,lon:1} 객체를 사용하여 각각매개변수에 이름을 줌
-------------------------------------------------------------------------------------------
확장성 , 재사용성 높이기
코드안에 하드코딩은 줄이고 매개변수를 많이 이용하자
기본값 반환은 재사용성을 해칠 수 잇다.
불필요한 가정은 코드 재사용을 해침 불필요한 가정이 많을숳록 일반적인 함수가 아니라는 뜻 재사용에 문제 가능성, 섣부른 최적화라 부른다. 최적화를 할 것이냐 vs 재사용을 높일것이냐
열거형 값을 암묵적으로 처리하지말자 케이스별로 처리를 하자 switch
값이 없을 때 널 을 반환 또는 에러를 반환하게 해야함 만약 이게 아니라 임의로 정한 디폴트값이나 아무값을주면 x 기본값과 값이 없는것은 엄연히 다르다
중요한 값은 옵셔널이면 안된다.
웬만하면 const를 사용하고 웬만하면 값 복제를 이용 특히 배열이나 객체 (



